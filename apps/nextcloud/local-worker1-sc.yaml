apiVersion: storage.k8s.io/v1
kind: StorageClass
metadata:
  name: local-worker1
provisioner: openebs.io/local
parameters:
  # Pfad auf worker1 wo die SSD gemountet ist
  basePath: "/var/openebs/k3s-storage"
volumeBindingMode: WaitForFirstConsumer
reclaimPolicy: Retain
