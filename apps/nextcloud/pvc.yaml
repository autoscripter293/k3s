apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nextcloud-pvc
  namespace: nextcloud
  labels:
    app: nextcloud
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-ssd-nextcloud
  resources:
    requests:
      storage: 300Gi
